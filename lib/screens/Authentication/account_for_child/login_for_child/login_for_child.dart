import 'package:flutter/material.dart';

class LoginForChild extends StatelessWidget {
  const LoginForChild({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
